[phases.setup]
nixPkgs = ["nodejs-22_x", "pnpm-9_x"]

[phases.install]
cmds = [
  "cd caiovieira-landing",
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "cd caiovieira-landing",
  "pnpm run build"
]

[start]
cmd = "cd caiovieira-landing/dist && caddy file-server --listen :80"